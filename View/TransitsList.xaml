<Window x:Class="WpfApp1.View.TransitsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="TransitsList" Height="450" Width="800">
    <Grid>
        <TreeView>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Aspects}">
                    <TextBlock Text="{Binding Info}" />
                    <!-- This is the header -->
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.Resources>
                <DataTemplate DataType="{x:Type local:Group}">
                    <TextBlock Text="{Binding PointA.PointName}" />
                    <!-- This displays Info for each Aspect -->
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</Window>
