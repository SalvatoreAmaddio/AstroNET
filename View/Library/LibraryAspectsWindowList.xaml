<Window x:Class="WpfApp1.View.LibraryAspectsWindowList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        xmlns:converter="clr-namespace:WpfApp1.converter"
        xmlns:fr="clr-namespace:FrontEnd.Forms;assembly=FrontEnd"
        xmlns:controller="clr-namespace:WpfApp1.controller"
        Title="LibraryAspectsWindow" 
        ResizeMode="NoResize"
        Height="450" Width="450">

    <Window.Resources>
        <converter:GetStar x:Key="getStar"/>
        <converter:EnergyConverter x:Key="getEnergy"/>
    </Window.Resources>
    
    <fr:FormList HeaderRow="0">
        <fr:Lista ItemsSource="{Binding RecordSource}" SelectedItem="{Binding CurrentRecord}">
            <fr:Lista.Header>
                <Grid fr:Definition.ColumnDefinitions="100,100,100,100">
                    <fr:HeaderFilter Grid.Column="1" Text="STAR" IsWithinList="True" ItemsSource="{Binding StarOptions}"/>
                    <fr:HeaderFilter Grid.Column="2" Text="ASPECT" IsWithinList="True" ItemsSource="{Binding EnergyOptions}"/>
                    <fr:HeaderFilter Grid.Column="3" Text="STAR" IsWithinList="True" ItemsSource="{Binding StarOptions2}"/>
                    <Label Grid.Column="4" Content="COMMANDS"/>
                </Grid>
            </fr:Lista.Header>
            <fr:Lista.ItemTemplate>
                <DataTemplate>
                    <fr:FormRow>
                        <Grid fr:Definition.RowDefinitions="30" fr:Definition.ColumnDefinitions="100,100,100,50,50">
                            <Label Grid.Column="0" Content="{Binding Star.PointId, Converter={StaticResource getStar}}"/>
                            <Label Grid.Column="1" Content="{Binding Energy.EnergyId, Converter={StaticResource getEnergy}}"/>
                            <Label Grid.Column="2" Content="{Binding Star2.PointId, Converter={StaticResource getStar}}"/>
                            <fr:DeleteButton Grid.Column="3" IsWithinList="True"/>
                            <fr:OpenButton Grid.Column="4" IsWithinList="True"/>
                        </Grid>
                    </fr:FormRow>
                </DataTemplate>
            </fr:Lista.ItemTemplate>
        </fr:Lista>
    </fr:FormList>
</Window>
